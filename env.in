# Go to https://kubernaut.io/token in order to obtain a kubernaut token:
KUBERNAUT_TOKEN=YOUR_TOKEN_HERE

# AWS credentials (id and access key) that allow access to an ECR
# registry appropriate for integration testing:
FORGE_ECR_KEY_ID=YOUR_TEST_AWS_ACCESS_KEY_ID
FORGE_ECR_SECRET_KEY=YOUR_TEST_AWS_SECRET_ACCESS_KEY

# AWS credentials for deploying a release. These are only needed for
# uploading a release artifact. Just removing them for testing only.
DEPLOY_KEY_ID=YOUR_DEPLOY_AWS_ACCESS_KEY_ID
DEPLOY_KEY=YOUR_DEPLOY_AWS_SECRET_ACCESS_KEY
